rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // Releases: public download, reviewer upload
    match /releases/{file} {
      allow read: if true;
      allow write: if request.auth != null
        && request.auth.token.role == 'reviewer';
    }
  }
}
